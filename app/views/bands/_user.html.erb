<%= form_for(@band) do |f| %>
  <% if @band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
      <% @band.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>%
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :user_rating, "How much do you like #{@band.name}:", class: "form-control-label" %><br>
    <%= f.number_field :user_rating, in: 1...6, class: "form-control" %>
    <%= f.hidden_field :user_id, :value => current_user.id %> <br>
    <%= f.submit 'Submit Quality Rating', :class=> 'btn btn-success' %>
  </div>  
<% end %>
